# 贡献指南 / Contributing

感谢你对 `rl-from-scratch` 的兴趣！这个库的目标是把强化学习主流算法的
**数学推导 + 从零实现 + 可运行验证**钉在一起，作为一个诚实、可复现的学习资源。
欢迎任何让它更清晰、更正确、更好用的贡献。

## 可以贡献什么

- **修 bug**：代码、公式、错别字都算。
- **补充推导**：某一步跳得太快、某个直觉没讲清楚，欢迎补。
- **新算法章节**：遵循现有章节结构（见下）。
- **实验复现**：换随机种子/环境跑，报告你的结果。
- **可视化**：更好的学习曲线、示意图。

## 每一章的结构约定

每个章节目录应包含：

```
NN_topic/
├── README.md      # 数学推导（不跳步）+ 直觉/比喻 + 本章 demo 说明
└── *.py           # 单文件、可直接 python 运行的从零实现
```

代码风格：

- **单文件可运行**：`python NN_topic/xxx.py` 就能看到结果，不依赖复杂框架。
- **只用 numpy / PyTorch / gymnasium**（见 `requirements.txt`）。
- **固定随机种子**，保证结果可复现；README 里报告的数字必须来自真实运行。
- 关键公式在代码注释里用 `公式 (n)` 与 README 对应。
- 变量命名对齐 [`notation.md`](notation.md) 的统一符号表。

## 提交前自检

```bash
# 1) 语法检查
python -c "import compileall; compileall.compile_dir('.', quiet=1)"

# 2) 冒烟测试（表格章节，秒级）
python tests/smoke_test.py

# 3) 如果你改了某个 demo，跑一遍确认它仍能达标
python NN_topic/xxx.py
```

CI 会在每个 PR 上自动跑上面的 1) 和 2)。

## 提 PR 的流程

1. Fork 本仓库并新建分支：`git checkout -b fix/xxx`
2. 提交改动，commit message 说清「改了什么、为什么」。
3. 发起 PR，描述里附上你的运行结果（尤其是改了算法/超参时）。
4. 保持 PR 聚焦——一个 PR 只做一件事，方便 review。

## 诚实实验原则（重要）

本库的立身之本是「**所有报告的数字都来自真实运行**」。
请**不要**在 README 里写「预期效果很好」却没有实际跑过的结论。
如果某个算法在某设置下不 work，**如实写出来**——失败案例往往比成功更有教学价值
（例如第 11 章 RL 交易里 DQN 在测试段跑输买入持有，正是回测陷阱的活体演示）。

## 行为准则

对人友善，对事严谨。技术讨论对事不对人。
